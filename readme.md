# Installation

```
    git clone ssh://git@dev.nukacode.com:33989/tool/local-dash.git ./
    composer update
    sudo npm install
    bower install
    composer global require "laravel/envoy=~1.0"
```